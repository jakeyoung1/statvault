# Copy to .env and adjust. Homebrew Postgres defaults to your macOS username, no password.
DATABASE_URL=postgresql+psycopg2://USER@localhost:5432/statvault
# Number of API requests allowed per key per calendar month (default plan)
DEFAULT_MONTHLY_LIMIT=1000
# CORS origin for the React dev server
FRONTEND_ORIGIN=http://localhost:5173
